import { Controller } from '@nestjs/common';

/**
 * AppController is kept as a placeholder for top-level routes that don't fit
 * a feature module. Health checks live in `health/HealthController` (Phase A).
 */
@Controller()
export class AppController {}